# Known Issues

- iOS/PWAでは `navigator.vibrate()` が利用できない環境があり、ハプティックは端末・ブラウザ依存です。非対応時も操作は継続できます。
- Screen Wake Lockは対応ブラウザかつ画面表示中のみ有効です。バックグラウンド復帰時は再取得を試みます。
- 週間予定DBが未設定または取得できない場合は、API内のフォールバック予定を表示します。
